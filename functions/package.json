{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.1.1",
    "resend": "^6.9.3"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
